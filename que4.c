#include<stdio.h>
int main()
{
    float phy,mat,bio,chem,comp;
     printf("Enter the percentage marks obtained in all subjects",phy,mat,bio,chem.comp);
     scanf("%d %d %d %d %d %d", &phy,&mat,&bio,&chem,&comp);
     percentage= phy + mat+bio+chem+comp/5
     printf(%d\n,percentage);
     if (percentage >=90)
     {
        printf("grade A");
     }else if(percentage >80)
     {
        printf("grade B"):
     }
     else if(percentage >70)
     {
        printf("grade C");
     }
     else if(percentage>60)
     { 
         printf("grade D" );
     }
     else if (percentage >=40)
     {
         printf("grade E");
     }
      else if(percentage <40)
     { 
         printf("grade F");
     } 
     
     
     
    return 0;
}